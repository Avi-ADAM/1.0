# TIMEGRAMA — שעון ההבשלה של הריקמה

> **סטטוס: שלבים 0, 1, 4.1 ו-4.2 הושלמו ומחוברים ✅ 2026-08-25.
> B12 ו-B13 נפתרו — ההרשאות הוענקו. הכרעות D1–D5 התקבלו. טרם נפרס.**
>
> **B13 היה הממצא החשוב בסשן:** 13 מתוך 44 הפעולות שמטפלי ה-timegrama
> מבצעים היו חסומות בהרשאות — הסבר שני, בלתי-תלוי, לתלונה המקורית. גם
> זנים שכן היה להם מטפל נכשלו באמצע, בשקט.
> המסמך ממפה את המצב הקיים במלואו, מונה את התקלות עם ראיות (file:line
> **ומדידה חיה מול הפרודקשן**), ומציע ארכיטקטורת יעד + תכנית עבודה בשלבים
> שאפשר לעקוב אחריה לאורך כמה סשנים. כל פריט עבודה מסומן ב-`[ ]` ומיועד
> לסימון תוך כדי התקדמות.
>
> נכתב ב-2026-08-24. עודכן לאחרונה: 2026-08-25.

## 0. הכרעות שהתקבלו

| # | ההכרעה | תאריך |
|---|---|---|
| **D1** | **`maap` — חתימת האחראי היא תנאי הכרחי.** ההבשלה בשתיקה חלה רק כשהאחראי כבר חתם וחסרות רק תגובות שאר חברי הריקמה. בלי חתימתו — השעון נסגר וההצעה נשארת פתוחה. אותו דפוס בדיוק כמו `funderHasSigned` ב-stipend (כלל 8 ב-§6). | 2026-08-25 |
| **D2** | **`askwant` — ככל `ask`/`askm` שמשלבים מישהו מחוץ לריקמה:** אם אף חבר ריקמה לא אישר — **אין הבשלה**. אם חבר אחד או יותר אישרו והשאר שתקו — **יש הבשלה**. זה בדיוק החוזה של `computeNegoGate` (`src/lib/server/nego/negoGate.ts`), ולכן `askwant` מקבל מטפל אמיתי ולא ביטול. | 2026-08-25 |
| **D3** | **שרת ה-TIMEGRAMA רץ מכאן ואילך אך ורק על ה-VPS הייעודי** (Linux, אותו מארח כמו Strapi — ר׳ `docs/PLAN_PROXY_SECURITY.md` §5, §10). לא Vercel. משמעויות ב-§4.6. | 2026-08-25 |
| **D4** | נגזר מ-D3: **ה-cron בשליטתנו** (system cron / systemd timer על ה-VPS) ולא מוגבל למגבלות התזמון של Vercel. | 2026-08-25 |
| **D5** | **התור התקוע יבשיל לפי הכללים, לא ייסגר בשקט.** 17 ה-`tosplit` (וכל שאר היתומות) עוברים את שער ההבשלה הרגיל כאילו השעון פעל בזמנו — **אלא אם האובייקט אורכב בינתיים**, ואז אין מה להבשיל והשעון פשוט נסגר. כלומר אין "סקריפט ניקוי" שמסמן `done` בעיוורון: המטפל שנכתוב בשלב 4 הוא גם מה שמנקה את ההיסטוריה. עונה על **Q7**. | 2026-08-25 |

---

## 1. למה המסמך הזה קיים

עברנו למערכת ה-**Action** (`src/lib/server/actions/`) כדי שלכל פעולה יהיה
מקום אחד, חתימה אחת, שער הרשאות אחד, וכדי שאפשר יהיה לקרוא לה מכל מקום —
לקוח, שרת, API חיצוני, cron. היום יש ~140 קונפיגורציות פעולה ורישום אחד
(`registry.ts`).

**TIMEGRAMA היא היוצאת מן הכלל היחידה שנשארה.**

`timegrama` הוא "שעון ההבשלה": הרשומה שאומרת *"בתאריך X, אם אף אחד לא ענה,
הגרסה שעל השולחן מאושרת"*. זה הביטוי הטכני של אחד מעקרונות-העל של המערכת —
**שתיקה היא הסכמה, בקצב הריקמה**. כלומר כמעט כל זרימת ההסכמה במערכת
(`ask`, `askm`, `pendm`, `pmash`, `decision` על כל הזנים שלו, `finiapruval`,
`stipend_payment`, רדימות, ארכוב בסוף מחזור) תלויה בו.

ובכל זאת:

- הקוד שמריץ אותו יושב ב-`src/routes/api/timegrama/` בעשרה קבצי
  `.svelte` (שאינם קומפוננטות — הם `<script module>` בלבד), עם GraphQL גולמי
  משורשר בתוך תבניות מחרוזת, ולא עובר דרך `actionService` בכלל.
- הלוגיקה בכל אחד מהם **משוכפלת** מתוך ה-Action המקבילה (`ask.svelte` מול
  `finalizeAskAcceptance.ts`, `finiapp.svelte` מול `closeFiniapruval.ts`,
  `pend.svelte` מול `voteOnPendm.ts`). שתי הדרכים יכולות להתפצל — ולמעשה
  כבר התפצלו.
- **חלק מהזנים אין להם קוד בכלל.** ה-cron מושך אותם, לא יודע מה לעשות
  איתם, לא מסמן `done`, וחוזר עליהם שוב בעוד שעה. לנצח.
- אין שום מדד, שום לוג מובנה, שום שדה שגיאה. timegrama שנכשל נראה בדיוק
  כמו timegrama שממתין.

זה בדיוק התיאור שהתלוננו עליו: *"הכרטיס פשוט נדחף שעה ועוד שעה"*.

---

## 2. מפת המצב הקיים

### 2.1 שרשרת ההפעלה

```
cron חיצוני  ──►  GET /api/pingrama            src/routes/api/pingrama/+server.js
                    │  (REND_URL || https://api.1lev1.com/)
                    │  fetch עם AbortController של 10 שניות
                    ▼
                  GET {REND}/api/timegrama      src/routes/api/timegrama/+server.js
                    │
                    │  שאילתה אחת: timegramas(done:{ne:true}, date:{lte: now+1h})
                    │  → לכל רשומה: x(id, whatami, tgid, fetch)
                    │     • date עבר      → קריאה מיידית (לא ב-await!)
                    │     • date בעתיד    → setTimeout עד שעה קדימה
                    ▼
                  x()  ──►  מפזר if/else ל-10 מטפלים
```

אין קונפיגורציית cron בריפו — ההפעלה חיצונית לחלוטין. אין אימות על ה-endpoint
(`GET /api/timegrama` פתוח).

מסלול נפרד ומקביל: `GET /api/monthi` (`src/routes/api/monthi/+server.js`) פותח
מחזורי `Maap` חודשיים ו**יוצר** להם timegrama — אבל אף אחד לא סוגר אותם
(ר׳ B2).

### 2.2 עשרת המטפלים

| קובץ | `whatami` | מה עושה | סגנון גישה לנתונים |
|---|---|---|---|
| `pend.svelte` | `pendm` | משימה ממתינה → OpenMission (+Ask למיועד, +match-suggestions) | `SendToAdmin` + GraphQL גולמי |
| `pendM.svelte` | `pmash` | משאב ממתין → OpenMashaabim (+match-suggestions) | `SendToAdmin` + GraphQL גולמי |
| `ask.svelte` | `ask` | מועמדות למשימה → Mesimabetahalich + צירוף חבר | `SendToAdmin` + GraphQL גולמי |
| `askm.svelte` | `askm` | מועמדות למשאב → `runResourceAskmAcceptance` | `strapiClient` + qids ✅ |
| `finiapp.svelte` | `finiapruval` | אישור שעות → FinnishedMission | `SendToAdmin` + GraphQL גולמי |
| `decision.svelte` | `decision` | כל זני ה-Decision (saleClaim, archiveObject/editObject, stipend*, pic/name/…) | מעורב: `SendToAdmin` + `execFromAdmin` |
| `mesimabetahalich.svelte` | `mesimabetahalich` | שעון רדימות → `openDormancyProposal` | `execFromAdmin` ✅ |
| `archiveEffective.svelte` | `mashabetahalich`, `matanot` | ארכוב בסוף מחזור | `SendToAdmin` + GraphQL גולמי |
| `stipendPayment.svelte` | `stipend_payment` | "הכסף הגיע?" → confirmed on silence | `SendToAdmin` + GraphQL גולמי |

שלושה סגנונות גישה שונים לאותו backend, ואף אחד מהם אינו `actionService`.

### 2.3 האינוונטר המלא — מי יוצר, מי מטפל, מי הפעולה המקבילה

| `whatami` | יוצר (producer) | מטפל ב-`x()` | Action מקבילה קיימת | סטטוס |
|---|---|---|---|---|
| `ask` | `applyToMission.ts:417`, `createMission.ts:278`, `nego/timegrama.ts:81`, `pend.svelte:199`, `archive/pendingOffers.ts:395` | ✔ `Ask` | `finalizeAskAcceptance` | **משוכפל** |
| `askm` | `availiableResorce/[id]/+page.svelte:209`, qid `127`, `runResourceAskmAcceptance.ts` | ✔ `Askm` | `finalizeAskmAcceptance` | ✅ תקין (משתמש ב-helper משותף) |
| `pendm` | `createMission.ts`, qid `165` | ✔ `Pend` | `voteOnPendm` | **משוכפל** |
| `pmash` | `createResource.ts:555` | ✔ `PendM` | `voteOnPmash` | **משוכפל + באג** (B4) |
| `finiapruval` | `completeMission.ts:173`, `timerSave.ts:203`, `timers/flushRateChange.ts:336` | ✔ `finiapp` | `closeFiniapruval` | **משוכפל + באגים** (B3, B5) |
| `decision` | `counterSaleClaim.ts:90`, `createSale.ts:268`, `createDonationSale.ts:144`, `updateProjectDetails.ts:144`, `archive/decision.ts:161`, `archive/vote.ts:223`, `stipend/decision.ts:158,227` | ✔ `Decision` | `voteOnDecision` | **משוכפל** (הכי מסוכן — 12+ זנים) |
| `mesimabetahalich` | `archive/dormancyClock.ts:70` | ✔ `Dormancy` | — | ✅ תקין |
| `mashabetahalich` | `archive/apply.ts:329` | ✔ `ArchiveEffective` | — | ✅ תקין |
| `matanot` | `archive/apply.ts:329` | ✔ `ArchiveEffective` | — | ✅ תקין |
| `stipend_payment` | `settleStipendCycle.ts:154` | ✔ `StipendPayment` | `confirmStipendPayment` | **משוכפל** |
| **`maap`** | `monthi/+server.js:342`, qid `mrCreateCycleTimegrama` | ❌ **אין** | `voteOnMaap` | 🔴 **יתום** (B2) |
| **`tosplit`** | `prPr/whowhat.svelte:841` | ❌ **אין** | `addVote` (type=`tosplit`) | 🔴 **יתום** (B2) |
| **`sheirutpend`** | `prPr/sheirut/addSheirut.svelte:82` | ❌ **אין** | `addVote` (type=`sheirutpend`) | 🔴 **יתום + לא נוצר בפרודקשן** (B2, B6) |
| **`askwant`** | `prPr/sheirut/sheirutShow.svelte:57` | ❌ **אין** | — | 🔴 **יתום + לא נוצר בפרודקשן** (B2, B6) |
| `act` | — (רק בסכמה + בשאילתה) | ❌ אין | — | ⚪ שדה מת |
| `actt` | — | ❌ אין | — | ⚪ שדה מת |
| `matanotpend` | — | ❌ אין, **וגם לא נשלף בשאילתה** | — | ⚪ שדה מת |
| `open_mission` | — | ❌ אין, **וגם לא נשלף בשאילתה** | — | ⚪ שדה מת |
| `open_mashaabim` | — | ❌ אין, **וגם לא נשלף בשאילתה** | — | ⚪ שדה מת |
| `timer` | — | ❌ אין, **וגם לא נשלף בשאילתה** | — | ⚪ שדה מת |

---

## 3. תקלות — ממופות, מדורגות ו**נמדדות**

> כל התקלות למטה נבדקו מול הפרודקשן ב-2026-08-25 (§8, שלב 0). כל שורה
> תקועה בתור מופתה לתקלה ספציפית — אין "לא ידוע":
>
> | הגורם | שורות תקועות | תקלה |
> |---|---|---|
> | זן ללא מטפל בכלל | **23 מתוך 29 (79%)** | **B2** |
> | היעד נמחק, הדיספצ'ר מדלג בלי לסמן | **3** | **B11** |
> | מטפל יצא מוקדם בלי `markDone` | **3** | **B3** |
> | escaping שבור | 0 (סיכון רדום) | B5 |
> | `done: null` | 0 ✅ | B10 |

### 🟡 B1 — התור בלי `pagination`, בלי `sort` ובלי גבול תחתון — **הופרך כגורם נוכחי, נשאר סיכון רדום**

`src/routes/api/timegrama/+server.js:59`

```graphql
timegramas (filters:{done:{ne: true}, date:{ lte: "…" }}) { data { … } }
```

אין `pagination`, אין `sort`, ואין גבול תחתון על `date` — כלומר **כל
timegrama תקוע מכל ההיסטוריה** מתחרה על מקומות באותו עמוד ברירת מחדל.

**המדידה (2026-08-25) הפריכה את ההשערה שזה הגורם הנוכחי:**

```
graphql.defaultLimit כאן ......... 250
עומס תקוע ופג-תוקף .............. 29
```

29 < 250, ולכן היום ה-cron **כן** רואה את כל התור בכל ריצה. זו לא הסיבה
שדברים לא קורים — הסיבה היא B2/B11/B3 (שהיו הופכים לבלתי-נראים אילו B1 היה
פעיל).

**אבל זה סיכון רדום אמיתי, ולכן נשאר בתכנית:** התור **גדל מונוטונית** — 23
מתוך 29 השורות הן זנים שאין להם מטפל ולכן לא ייסגרו לעולם. ברגע שהוא יעבור
250, המערכת כולה תיעצר בשקט מוחלט: אין שגיאה, אין לוג, פשוט "לא קרה כלום".
התיקון זול (שורה אחת) ולכן נעשה אותו ממילא בשלב 1, ביחד עם B2 שמפסיק את
הגידול.

---

### ✅ B13 — פער ההרשאות של טוקן ה-cron — **נפתר 2026-08-25**

התגלה 2026-08-25 תוך כדי בניית מטפל ה-`maap`. B12 (הרשאת `Haluka`) לא הייתה
מקרה בודד — היא הייתה הדגימה הראשונה של פער שיטתי. גזרתי מהקוד את **כל**
הפעולות ש-`src/routes/api/timegrama/*` וה-helpers שהם קוראים להם מבצעים
(44), ובדקתי כל אחת מול הפרודקשן עם `ADMINMONTHER`.

**13 חסומות:**

| פעולה | מי צריך אותה | מה שבור בפועל |
|---|---|---|
| `stipendPayment` (קריאה) | `stipendPayment.svelte` | 🔴 **המטפל מת לגמרי** — לא מצליח אפילו לקרוא את התשלום, מדווח "not found", **וסוגר את השעון**. אישור-בשתיקה של תשלום מלגה לא עבד מעולם, והשעון נמחק בלי להשאיר עקבות. |
| `updateStipendPayment` | `stipendPayment.svelte` | 🔴 אותו דבר |
| `createStipendPledge` | `decision.svelte` (stipend) | 🔴 `applyStandingStipend` — **הבשלת הצעת מלגה בשתיקה לא עבדה מעולם** |
| `createStipendProgram` | `decision.svelte` (stipend) | 🔴 אותו דבר |
| `createDecision` | `mesimabetahalich.svelte` | 🔴 `openDormancyProposal` — **שעון הרדימות לא עבד מעולם** |
| `updateMatanot` | `decision.svelte`, `archiveEffective.svelte` | 🔴 ארכוב מוצר בסוף מחזור, והתאמת מלאי אחרי saleClaim |
| `updateAct` | `ask.svelte` | 🟠 רשימת המשימות לא עוברת ל-Mesimabetahalich; ה-Acts נשארים יתומים |
| `createAsk` | `pend.svelte` (`offerToAssignee`) | 🟠 pendm מוקצה מבשיל ל-OpenMission — **אבל המיועד לא מקבל הצעה** |
| `rikmash` (קריאה) | `maap.svelte` | ⛔ חוסם את 4.1 |
| `createRikmash` | `maap.svelte` | ⛔ חוסם את 4.1 |
| `updateRikmash` | `maap.svelte` | ⛔ חוסם את 4.1 |
| `forum` (קריאה) | מסלול `askm` | 🟡 לבדוק אם המסלול באמת נוגע בו |
| `welcomTop` (קריאה) | `ask.svelte` | ⚪ ככל הנראה לא נדרש — `createWelcomTop` מותר, ורק הוא בשימוש |

**זה הסבר שני, ובלתי-תלוי, לתלונה המקורית.** עד כה הסברנו "דברים עם
timegrama לא קורים" בזנים בלי מטפל (B2). מסתבר שגם חלק מהזנים ש**כן** יש להם
מטפל נכשלים באמצע — בשקט, כי כמעט כל הקריאות עטופות ב-`catch` שמדפיס
ולא זורק. `stipend_payment` הוא המקרה החמור: הוא נכשל, **ובכל זאת סוגר את
השעון**, כך שהתשלום לא מאושר ואין יותר מנגנון שיחזור אליו.

**נפתר:** ההרשאות הוענקו ואומתו מחדש (קריאות ועדכונים בלבד — ר׳ האזהרה
למטה). כל 12 הפערים האמיתיים נסגרו. `welcomTop` (קריאה) נשאר חסום ו**זה
תקין**: אף מטפל לא קורא אותו, רק `createWelcomTop`, שמותר.

**כמה נזק זה הספיק לעשות בפועל:** לגבי `stipend_payment` — **אפס.** יש 0
תשלומי מלגה בפרודקשן, כלומר הבאג היה רדום וטרם נגע בנתונים. (בסקירה
הראשונה תיארתי אותו כאיבוד נתונים מתמשך; זה היה נכון עקרונית אך לא קרה.)
שאר המסלולים — רדימות, ארכוב מוצר בסוף מחזור, העברת רשימת משימות, הצעה
למיועד — פשוט לא פעלו מעולם, בלי לפגוע במה שכן נרשם.

**מה נדרש היה** (הרשאות ב-Strapi ל-role של `ADMINMONTHER`):

```
StipendPayment   find, update
StipendPledge    create
StipendProgram   create
Decision         create
Matanot          update
Act              update
Ask              create
Rikmash          find, create, update
Forum            find        (לאימות)
```

**איך לבדוק מחדש:** `scratchpad/tg-verify-perms.mjs` — **קריאות ועדכונים
בלבד.** שווה להעביר גרסה שלו ל-`scripts/` כבדיקה קבועה: פער הרשאות הוא
בדיוק סוג התקלה שאף טסט לא תופס, כי הוא חי בקונפיגורציה ולא בקוד.

> ⚠️ **אזהרה למי שיבדוק שוב — אל תבדקו הרשאות `create` עם מוטציות אמיתיות.**
> בסבב הראשון בדקתי `create<X>(data: {})` בהנחה שוולידציה תעצור אותן. לחלק
> מהסוגים אין שדות חובה, אז הן **הצליחו** והשאירו שורות ריקות —
> `timegrama` 349/350, `monter` 37/38, ו-2 `welcomTop` שאי אפשר אפילו לרשום.
> `delete` חסום לטוקן, כך שאי אפשר לנקות אותן מהקוד; **למחוק ידנית בפאנל.**
> הן אינרטיות (ל-timegramas יש `date: null` ולכן `lte` לא תופס אותן, ואוסף
> ה-`Monter` לא נקרא בשום מקום), אבל הן מנפחות את מונה ה-"לא done".
>
> הרשאות `create` ממילא לא חייבות בדיקה מקדימה: כל מטפל כותב את שורת
> הרישום שלו **לפני** שהוא מארכב משהו, ו-`strapi.execute` זורק על FORBIDDEN
> — כלומר הרשאה חסרה נכשלת בבטחה, לפני שנגעה בנתונים, והשעון נשאר פתוח.

---

### ✅ B12 — הרשאת `Haluka` לטוקן ה-cron — **נפתרה 2026-08-25**

התגלה 2026-08-25 תוך כדי בניית מטפל ה-`tosplit`. נבדק ישירות מול הפרודקשן:

| בדיקה | ADMINMONTHER | STRAPI_TOKEN | STRAPI_API_TOKEN | STRAPI_API_NEW | NEW_S | CONSENSUS_PUBLIC_TOKEN |
|---|---|---|---|---|---|---|
| `query halukas` | FORBIDDEN | FORBIDDEN | FORBIDDEN | FORBIDDEN | FORBIDDEN | FORBIDDEN |
| `query haluka(id)` | FORBIDDEN | — | — | — | — | — |
| `updateHaluka` | FORBIDDEN | — | — | — | — | — |
| `updateSale` / `updateTosplit` | ✅ | — | — | — | — | — |
| `usersPermissionsUser.hervachti` | ✅ | — | — | — | — | — |

כלומר: מכל ארבעת שלבי יישום החלוקה, השרת יכול לבצע שלושה — לסגור את
ה-Tosplit, לסמן מכירות `splited`, ולהזיז יתרות hervachti — ו**לא** את
הרביעי, סימון ה-Halukas כ-`ushar`. רק ה-JWT של המשתמש עצמו יכול, וזו
הסיבה ש-`approveHaluka` מהקלף עובד: הוא רץ עם `context.jwt`.

**למה זה חוסם:** הבשלה חלקית על מסלול כספי גרועה מאי-הבשלה. השילוב "החלוקה
סגורה, היתרות זזו, אבל ההעברות לא מסומנות כמגיעות" הוא מצב שאף מסלול אחר
במערכת לא מייצר, ואין ממנו דרך חזרה אוטומטית.

**נפתר:** ההרשאה הוענקה ל-`ADMINMONTHER` ואומתה מחדש — `query halukas`,
`query haluka(id)` ו-`updateHaluka` כולם עוברים. `tosplit` הוחזר
ל-`HANDLED_KINDS` והענף ב-`x()` חובר. שאר הטוקנים נשארו חסומים, וזה נכון:
רק הטוקן שה-cron משתמש בו צריך את זה.

**איתות רגרסיה:** אם ריצה תדפיס FORBIDDEN על `81updateHaluka` — ההרשאה
הזו היא מה שהתהפך.

**תופעת לוואי שכבר חיה היום:** `stipendPayment.svelte` מנסה
`updateHaluka(confirmed: true)` עם `ADMINMONTHER`, בתוך
`.catch(… 'haluka confirmation failed (non-fatal)')`. לפי הטבלה למעלה
הקריאה הזו **נכשלת בכל פעם** מאז שנכתבה — תשלום מלגה שמאושר בשתיקה לא
מסמן את ה-Haluka שלו כמאושרת, והאזהרה נבלעת בלוג. אותה הענקת הרשאה מתקנת
גם את זה.

---

### 🔴 B11 — יעד שנמחק משאיר את השורה תקועה לנצח

`src/routes/api/timegrama/+server.js:91-94`

```js
const relation = element.attributes[element.attributes.whatami];
if (!relation || relation.data == null) {
  console.warn(`timegrama ${element.id}: … has no relation data, skipping`);
  return;                      // ← אין markDone
}
```

כשה-`Decision`/`Ask`/`Askm` שאליו השעון מצביע נמחק, הדיספצ'ר מדפיס אזהרה
ו**יוצא בלי לסמן `done`**. השורה חוזרת בכל ריצה, לנצח, ומדפיסה את אותה
אזהרה — עוד רעש שמסתיר את האזהרות האמיתיות.

**אומת חי — 3 שורות, אחת מהן כבר 286 יום:**

```
tg#168  decision  381d  target MISSING
tg#192  ask       286d  target MISSING
tg#257  askm       83d  target MISSING
```

יעד שנמחק הוא המקרה הכי חד-משמעי שיש לסגירת השעון: אין מה להבשיל, לתמיד.

---

### 🔴 B2 — ארבעה זנים נוצרים ואף פעם לא מטופלים

`maap`, `tosplit`, `sheirutpend`, `askwant` — כולם נוצרים בקוד חי, כולם
נשלפים בשאילתה של `+server.js`, ולאף אחד אין ענף ב-`x()`
(`src/routes/api/timegrama/+server.js:20-50`).

התוצאה מדויקת: `x()` נקראת, לא נכנסת לשום `if`, מסיימת בלי לעשות דבר,
`done` נשאר `false` — ובעוד שעה הכול חוזר. **זה "נדחף שעה ועוד שעה"**, וזה
גם מה שמייצר בהתמדה את מלאי הרשומות התקועות שיפוצץ בבוא היום את B1.

**אומת חי (2026-08-25) — זו התקלה הדומיננטית, 23 מתוך 29 השורות התקועות:**

```
tosplit       17     ← ההצבעות על חלוקת רווחים
askwant        3     ← בקשות שירות (הישנה: 998 יום)
maap           2     ← מחזורי הוצאה חודשיים
sheirutpend    1     ← הצעות שירות (1001 יום)
```

`tosplit` הוא המספר שהכי צריך להדאיג: 17 חלוקות רווח שהוצעו, קיבלו שעון
הבשלה, ומעולם לא הבשילו.

לגבי `maap` יש טוויסט: ההערה ב-`monthi/+server.js:339` אומרת *"the cycle
auto-approves once the window elapses (clients cast the YES)"* — ואכן
`src/lib/components/lev/weget.svelte:161-163,277` מריץ `autoApprove()`
בצד הלקוח כשהשעון פג. כלומר מחזור חודשי מאושר רק אם **חבר ריקמה במקרה
פותח את עמוד הלב**. זו לא הבשלה, זו הגרלה. גם `tosplit` ו-`sheirutpend`
זוכים להצבעה דרך `addVote.ts` אבל אף מסלול שם לא סוגר את ה-timegrama.

---

### 🟠 B3 — נתיבי יציאה מוקדמים שלא מסמנים `done`

כשמטפל מחליט "אין מה לעשות כאן", עליו לסגור את השעון. אחרת הרשומה
נשארת בתור לנצח (ומזינה את B1).

| מקום | התנאי | סוגר `done`? |
|---|---|---|
| `finiapp.svelte:33` | ה-finiapruval לא נמצא | ❌ |
| `finiapp.svelte:38` | כבר `archived` | ❌ |
| `finiapp.svelte:43` | יש הצבעת "לא" | ❌ |
| `pendM.svelte:24` | ה-pmash כבר `archived` | ❌ |
| `pendM.svelte` (כל ה-`catch`) | כל שגיאה | ❌ |
| `ask.svelte:29` | ה-ask לא נמצא | ❌ |
| `ask.svelte:99` | שאילתה שנייה החזירה null | ❌ |
| `ask.svelte:178` | המוטציה החזירה null | ❌ |

`decision.svelte`, `stipendPayment.svelte`, `archiveEffective.svelte`,
`mesimabetahalich.svelte` ו-`askm.svelte` **כן** עושים זאת נכון — הם הדגם
לחיקוי.

**אומת חי (2026-08-25) — 3 שורות, כולן בדיוק בענף ה-`archived`:**

```
tg#17   pmash        1040d   pmash#42 archived=true   → pendM.svelte:24
tg#18   pmash        1033d   pmash#41 archived=true   → pendM.svelte:24
tg#127  finiapruval   522d   fini#79  archived=true   → finiapp.svelte:38
```

כלומר: ההצבעה **כן** עברה, האובייקט **כן** נסגר כמו שצריך — ורק השעון
נשאר תלוי באוויר ומתעורר כל שעה כדי לגלות שאין לו מה לעשות. כמעט שלוש
שנים, במקרה של #17.

---

### 🟠 B4 — `pendM.svelte` קורס על רשומה חסרה

`src/routes/api/timegrama/pendM.svelte:24`

```js
if (res.data.pmash.data.attributes.archived != true) {
```

אם ה-pmash נמחק, `res.data.pmash.data` הוא `null` → TypeError → נבלע
ב-`catch` → אין `done` → תקוע לנצח. `pend.svelte:27` כבר תוקן לדפוס הנכון
(`?.` + יציאה עם `markDone`); `pendM.svelte` לא.

---

### 🟠 B5 — טקסט חופשי מוזרק ללא escaping לתוך מחרוזות GraphQL

`src/routes/api/timegrama/finiapp.svelte:87-88,115-116`

```js
missionName: "${fa.missname}",
why: "${fa.why ?? ''}",
```

גרשיים, שורה חדשה או `\` בתוך `why` (טקסט שהמשתמש הקליד!) שוברים את
המוטציה. התוצאה: שגיאת GraphQL, אין `done`, ניסיון חוזר כל שעה — **לנצח,
כי הקלט לא ישתנה**. זה בדיוק "מתקבלות שגיאות".

`decision.svelte` כבר פתר את זה נכון עם `gqlStr()` (שורות 17-19);
`ask.svelte` משתמש ב-block strings (`"""…"""`) שעמידים יותר אך עדיין
שבירים מול `"""` בתוכן. `finiapp.svelte` לא מוגן כלל.

**סטטוס מדידה: סיכון רדום, לא הגורם לאף שורה תקועה כרגע.** ה-finiapruval
היחיד שתקוע (#79) מכיל `why="https://event-by-click.vercel.com"` —
נקי מגרשיים ומשורות חדשות, כלומר הוא תקוע מ-B3 ולא מכאן. אבל השדה הוא
טקסט חופשי של משתמש, ולכן זו פצצה מתקתקת: המוטציה תישבר על התו הראשון
שיישבר, והתור ינסה מחדש כל שעה **לנצח, כי הקלט לא ישתנה**. תיקון של שורה.

---

### 🟠 B6 — `sheirutpend` / `askwant` נוצרים ב-GraphQL גולמי מהלקוח → 403 בפרודקשן

`addSheirut.svelte:82` ו-`sheirutShow.svelte:57` קוראים ל-`SendTo(query)`,
ש-`src/lib/send/sendTo.svelte` שולח כ-`{data:{query}}` — כלומר בלי `queId`.
`src/routes/api/send/+server.js:80`:

```js
if (!isDev) throw error(403, 'Raw GraphQL queries are not allowed; use a queId');
```

כלומר בפרודקשן **גם היצירה עצמה נכשלת**, לא רק ההבשלה. שני הזרימות האלה
צריכות מיגרציה ל-Action בכל מקרה — ר׳ שלב 4.

---

### 🟡 B7 — הדיספצ'ר לא ממתין למטפלים

`src/routes/api/timegrama/+server.js:101`

```js
x(myid, element.attributes.whatami, tgid, fetch);   // ללא await
```

בתוך `await Promise.all(all.map(async …))` — אבל הקריאה עצמה לא ב-`await`,
אז `Promise.all` נפתר לפני שמשהו באמת רץ, ו-`GET` מחזיר `'Hello Cron!'`
מיד. תחת `adapter-node` העבודה בדרך כלל תסתיים; תחת כל ריצה serverless
או restart של הקונטיינר היא נקטעת באמצע — אחרי מוטציה ראשונה ולפני
`markDone`.

בנוסף, `setTimeout` של עד שעה (`:107`) מחזיק עבודה בזיכרון התהליך: deploy,
crash או flip של blue/green מוחקים אותה בלי זכר.

---

### 🟡 B8 — אין הגנת ריצה כפולה (idempotency)

אין נעילה, אין `claimedAt`, אין בדיקת `done` מחדש לפני הכתיבה. שתי ריצות
cron חופפות (או `setTimeout` מריצה קודמת + הריצה הנוכחית) יכולות לעבד את
אותה timegrama בו-זמנית ולייצר **שני** `Mesimabetahalich` לאותה מועמדות.

---

### 🟡 B9 — אין תצפית ואין מחזור חיים לכשל

ל-`Timegrama` יש בדיוק שני מצבים: `done: false` ו-`done: true`. אין
`attempts`, אין `lastError`, אין `lastAttemptAt`, אין `status`. לכן:

- אי אפשר להבדיל בין "ממתין" ל"נכשל 400 פעם".
- אין דרך לשאול "מה תקוע?" בלי לקרוא לוגים.
- אין backoff — כשל קבוע נדפק על ה-backend כל שעה עד קץ הימים.

---

### ⚪ B10 — פערים קטנים לתיעוד ובדיקה

- **`done:{ne:true}` ו-NULL — ✅ נבדק ונקי (2026-08-25).** ב-SQL `ne` מחריג
  שורות NULL, אבל הסכמה מגדירה
  `done: Attribute.Boolean & Attribute.DefaultTo<false>`
  (`src/lib/generated/contentTypes.d.ts:6931`) והמדידה החזירה **0 שורות**
  עם `done: null`. אין רשומות בלתי-נראות. לא נדרש טיפול.
- **`GET /api/timegrama` פתוח וללא אימות.** כל אחד יכול להריץ את מחזור
  ההבשלה. בשילוב עם B8 זה וקטור לריצות כפולות. **נסגר במסגרת D3** — ר׳ §4.6.
- **רעש בלוגים.** `+server.js:78,80,89,93` מדפיסים payload-ים שלמים בכל
  ריצה, מה שהופך את הלוג לבלתי-קריא בדיוק כשצריך אותו.
- **חמישה שדות יחס מתים** בסכמה (`act`, `actt`, `matanotpend`,
  `open_mission`, `open_mashaabim`, `timer`) — שלושה מהם אפילו לא נשלפים,
  כך שאם מישהו ייצור אחד כזה הוא ייפול ל-`console.warn` בשורה 93 ויתקע.

---

## 4. ארכיטקטורת יעד

> **העיקרון:** `timegrama` הוא **תור מתוזמן**, לא מקום שבו נכתבת לוגיקה
> עסקית. הלוגיקה שייכת ל-Action. מטפל timegrama הוא מתאם דק בלבד:
> *קרא את המצב → החלט אם הגרסה שעל השולחן ראויה להבשלה → קרא ל-Action →
> סמן את השעון*.

### 4.1 עיקרון ההרשאה — principal חדש: `timeout`

הבשלת שתיקה אינה פעולה של אף משתמש. היום המטפלים "מתחזים" למשתמש
(`askm.svelte` בונה `acceptCtx = { userId: takerId }`) או עוקפים את שכבת
ההרשאות לגמרי דרך `SendToAdmin`. שניהם רעים: הראשון משקר ביומן, השני
מוותר על השער.

ההצעה: `PrincipalKind` חדש `'timeout'`
(`src/lib/server/authz/types.ts:11`), שמשמעותו *"שעון הריקמה, לא אדם"*.
Action שרוצה להיות ניתנת להבשלה מצהירה עליו ב-`access`, בדיוק כמו
ש-`apiKey` הוצג בעבר. זה נותן:

- שער סטטי אמיתי גם על המסלול הזה (היום: אין);
- רשימה מפורשת וניתנת לביקורת של "מה מותר לזמן לעשות לבד";
- `confirmedBy: 'timeout' | 'silence'` בנתונים ממשיך לשקף את אותו מקור.

### 4.2 רישום זנים במקום `if/else`

`src/routes/api/timegrama/kinds.ts` — טבלה אחת שהיא מקור האמת:

```ts
export const TIMEGRAMA_KINDS = {
  ask:        { relation: 'ask',        handler: matureAsk },
  askm:       { relation: 'askm',       handler: matureAskm },
  pendm:      { relation: 'pendm',      handler: maturePendm },
  // …
} satisfies Record<string, TimegramaKind>;
```

ומכאן נגזרים אוטומטית: רשימת ה-relations שהשאילתה שולפת, הדיספצ'ר, ובדיקה
(`kinds.test.ts`) שמוודאת שכל `whatami` שמישהו **יוצר** בקוד קיים בטבלה.
זה הופך את B2 לבלתי-אפשרי מבנית.

### 4.3 מחזור חיים מפורש לרשומה

הרחבת `Timegrama` בסכמת Strapi:

| שדה | טיפוס | תפקיד |
|---|---|---|
| `status` | enum `pending \| running \| done \| failed \| skipped` | מצב אמיתי במקום בוליאני |
| `attempts` | Integer (default 0) | כמה פעמים נוסה |
| `lastAttemptAt` | DateTime | מתי לאחרונה |
| `lastError` | Text | הודעת השגיאה האחרונה |

`done` נשמר כפי שהוא לתאימות לאחור (`status:'done'` ⇒ `done:true`).

כללי הריצה: `attempts >= 5` ⇒ `status:'failed'` ויציאה מהתור (עם התראה),
backoff מעריכי בין ניסיונות, ו-`running` + `lastAttemptAt` כנעילה רכה נגד
ריצה כפולה (B8).

### 4.4 עיבוד אמין

- `for … of` עם `await` במקום `Promise.all` בלי await (B7).
- **ויתור מלא על `setTimeout` הארוך**: מריצים רק מה שהגיע זמנו, נקודה.
  זה אפשרי בזכות D4 (ר׳ §4.6) — cron כל 5-10 דקות עולה כלום כשהוא רץ
  מול Strapi מקומי, והדיוק שהוא נותן טוב יותר מזה של `setTimeout` שגם
  ככה לא שורד deploy.
- `pagination` מפורשת + `sort: "date:asc"` + עיבוד עד תקרה לריצה, כך
  שהתור מתנקז ולא נחסם (B1).

### 4.6 מיקום הריצה — VPS בלבד (D3/D4)

ההכרעה: `/api/timegrama` הוא **תהליך של ה-VPS**, לא של Vercel.
(`docs/PLAN_PROXY_SECURITY.md` §5: SvelteKit רץ על אותו VPS כמו Strapi,
ש-bind ל-`127.0.0.1`; §10: `x-strapi-gate` נועל את הגישה מבחוץ.)

מה זה משנה מעשית — כל אחד מהסעיפים סוגר תקלה שהייתה בתכנית:

| היבט | לפני (Vercel/Render) | אחרי (VPS) |
|---|---|---|
| מרחק ל-Strapi | דרך האינטרנט ל-`tovmeod` | loopback `127.0.0.1:1337` |
| תקציב זמן לריצה | חתוך; לכן `x()` בלי `await` "עבד" | תהליך node ארוך-חיים — **B7 נפתר בחינם** |
| שליטה בתזמון | מה ש-Vercel cron מרשה | system cron / systemd timer — **D4** |
| תדירות ריאלית | שעה | 5-10 דקות ⇒ `restime` מדויק יותר, בלי `setTimeout` |
| חשיפה | endpoint ציבורי | לקשור ל-loopback / לדרוש סוד — **סוגר את B10.2** |

נגזרות לביצוע:

- ה-endpoint יפסיק להיות ציבורי: או `127.0.0.1` בלבד ב-nginx, או header
  סוד באותו דפוס של `x-strapi-gate` (`STRAPI_GATE_KEY`). זו התשובה ל-Q4.
- **`/api/pingrama` הופך למיותר.** הוא נבנה כדי ש-Vercel יעיר אינסטנס
  Render רדום (`src/routes/api/pingrama/+server.js` — `REND_URL`, timeout
  של 10 שניות, `mode=wake`). כשה-cron יושב על אותו מארח, ה-proxy הזה הוא
  רק נקודת כשל נוספת. לפרק אחרי המעבר.
- אותו שיקול חל על `/api/monthi` — הוא cron באותה מידה, ועובר יחד איתו.
- לתעד את הפריסה ב-`docs/DEPLOY_API_DOCKER.md` (הקונטיינר כבר שם) כדי
  שה-timer לא ילך לאיבוד בפריסה הבאה.

### 4.5 תצפית

`GET /api/timegrama?dry=1` שמחזיר JSON: כמה ממתינים, כמה נכשלו, פילוח לפי
`whatami`, ועשרת הישנים ביותר. זה הכלי שיאפשר לענות "מה תקוע?" בלי לוגים,
וגם מה שיאמת את B1 בשלב 0.

---

## 5. תכנית עבודה

> סדר מכוון: קודם **לראות**, אחר כך **לעצור את הדימום**, ורק אז **לאחד**.
> אין טעם למגר לוגיקה ל-Action בזמן שהתור חסום — נשקיע יום ולא נראה שינוי.

### שלב 0 — אבחון ✅ **הושלם 2026-08-25**

- [x] 0.1 ספירה מול Strapi + פילוח לפי `whatami` → **29 תקועות, 23 מהן יתומות**.
- [x] 0.2 `graphql.defaultLimit` בפועל → **250**. 29 < 250 ⇒ **B1 הופרך כגורם נוכחי**, נשאר סיכון רדום.
- [x] 0.3 `done: null` → **0 שורות** ⇒ **B10 נקי**, לא נדרש טיפול.
- [x] 0.4 פילוח סיבתי מלא של כל 6 השורות מזנים מטופלים → **B3** ×3, **B11** ×3 (תקלה חדשה).
- [x] 0.5 תיעוד ביומן הריצה (§8).

> הסקריפטים: `scratchpad/tg-diag.mjs`, `tg-diag2.mjs` (קריאה בלבד). שווה
> להכניס גרסה מנוקה מהם ל-`scripts/` כחלק מ-3.4 (`?dry=1`).

### שלב 1 — עצירת הדימום ✅ **הושלם 2026-08-25**

- [x] 1.1 `+server.js`: `pagination:{limit:500}` + `sort:"date:asc"` — **B1** (רדום, זול).
- [x] 1.2 `+server.js`: `await x(…)` ולולאת `for…of` במקום `Promise.all` — **B7**.
- [x] 1.3 `+server.js`: יעד חסר ⇒ `markDone` — **B11**, סוגר 3 שורות.
- [x] 1.4 `+server.js`: `whatami` לא מוכר ⇒ `warn` + `markDone` — **בשינוי, ר׳ למטה**.
- [x] 1.5 `finiapp.svelte`: `markDone` בשלושת נתיבי היציאה — **B3**.
- [x] 1.6 `finiapp.svelte`: `gqlStr()` על `missname` ו-`why` — **B5**.
- [x] 1.7 `pendM.svelte`: optional chaining + `markDone` בכל נתיב — **B3, B4**.
- [x] 1.8 `ask.svelte`: `markDone` בנתיבי היציאה — **בשינוי, ר׳ למטה**.
- [x] 1.9 הורדת לוגי ה-payload, שורת סיכום אחת לריצה — **B10**.
- [x] 1.10 `npm run check` (0 שגיאות בקבצי timegrama) + syntax check + `npm test`.

**שני שינויים מהתכנית המקורית — שניהם מכוונים:**

**1.4 — לא סוגרים את ארבעת הזנים היתומים.** התכנית אמרה "`whatami` לא מוכר
⇒ `markDone`", אבל זה היה סוגר בשקט את 23 השורות היתומות — ובהן **17 הצעות
חלוקת רווח אמיתיות** — ובכך מכריע את **Q7** בלי לשאול. במקום זה יש שתי
קבוצות ב-`+server.js`:

| קבוצה | התנהגות |
|---|---|
| `HANDLED_KINDS` (10) | רצות כרגיל |
| `AWAITING_HANDLER` — `maap`, `tosplit`, `sheirutpend`, `askwant` | **נשארות פתוחות** בשקט עד שלב 4; ההצעה עומדת |
| כל השאר (שדות מתים, typo) | `warn` + `markDone` |

חריג אחד ומכוון: זן יתום שה**יעד שלו נמחק** כן נסגר — אין הצעה עומדת
שאפשר לאבד.

**1.8 — ב-`ask.svelte` רק 2 מתוך 3 נתיבי היציאה נסגרים.** השלישי
(`res3.data == null`) הוא ה**כתיבה** שמבצעת את ההבשלה, אחרי ששני הצדדים
כבר הסכימו. סגירת השעון שם הייתה זורקת תוצאה מוסכמת לפח. לכן הוא מדווח
`console.error` ומשאיר את השורה לניסיון חוזר — בדיוק החריג שכלל 4 ב-§6
מתיר. המחיר (ניסיון חוזר בלי תקרה ובלי תיעוד) הוא בדיוק מה ש-`attempts`/
`lastError` פותרים בשלב 5.

**תוספת שלא הייתה בתכנית:** השאילתה לא שלפה 4 משדות היחס
(`matanotpend`, `open_mission`, `open_mashaabim`, `timer`). שדה שלא נשלף
נראה בדיוק כמו יעד שנמחק — כלומר תיקון B11 היה סוגר שעון חי בטעות. כל
20 השדות נשלפים עכשיו.

> **מה שלב 1 משיג בפועל:** 6 מ-29 השורות ייסגרו בריצה הראשונה (3 יעדים
> מחוקים + `pmash` #17/#18 + `finiapruval` #127), והזליגה מהזנים המטופלים
> נעצרת. 23 היתומות ממתינות במכוון לשלב 4.

### שלב 2 — ניקוי התור הקיים — **בוטל ומוזג לשלב 4 (D5)**

~~סקריפט חד-פעמי שמסמן `done:true` ליתומות.~~ **D5 ביטל את הגישה הזו:**
התור התקוע אינו זבל שצריך לגרוף אלא הצעות שממתינות להכרעה, ולכן **המטפל
שנכתב בשלב 4 הוא גם מה שמנקה את ההיסטוריה** — כל שורה עוברת את שער
ההבשלה הרגיל, ומי שאורכב בינתיים פשוט נסגר. אין סקריפט ניקוי.

מה שכן נשאר לעשות, אחרי כל מטפל חדש:

- [ ] 2.1 להריץ מחדש את `tg-diag.mjs` ולוודא שהזן שטופל התנקז.
- [ ] 2.2 לתעד ביומן הריצה כמה שורות הבשילו וכמה נסגרו כ"כבר סגור".

### שלב 3 — רישום זנים + תצפית (סשן אחד)

- [ ] 3.1 `src/routes/api/timegrama/kinds.ts` עם הטבלה מ-§4.2.
- [ ] 3.2 `+server.js` נגזר מהטבלה (שאילתה + דיספצ'ר).
- [ ] 3.3 `kinds.test.ts`: סריקת כל `whatami: '…'` בקוד ואימות שהוא רשום.
- [ ] 3.4 `?dry=1` עם סיכום JSON — **§4.5**.

### שלב 4 — סגירת הזנים היתומים (1-2 סשנים) — **B2**, 79% מהתור

- [x] 4.1 `maap` → **הושלם ומחובר 2026-08-25.** נחסם זמנית על B13 עד שהוענקה הרשאת `Rikmash`. נבנו: `src/lib/server/recurring/settleCycleMaap.ts` (חילוץ מ-`voteOnMaap`, 7 טסטים) + `src/routes/api/timegrama/maap.svelte`; `archived` נוסף ל-qid `151getMaapForVote`; ההבשלה-בדפדפן הוסרה מ-`weget.svelte`. מטפל **מותנה בחתימת האחראי (D1)**: אין חתימה ⇒ סוגרים את השעון ומשאירים את המחזור פתוח, בדיוק כמו `funderHasSigned` ב-`decision.svelte`. להסיר את ה-`autoApprove()` הלקוחי מ-`weget.svelte:277` — הבשלה היא לא פונקציה של מי פתח את הדפדפן.
- [x] 4.2 `tosplit` → **הושלם ומחובר 2026-08-25.** הזן הגדול ביותר בתור (17 שורות). נחסם זמנית על B12 עד שההרשאה הוענקה. מה נבנה:
  - `src/lib/server/haluka/applyTosplitApproval.ts` — **חילוץ** של ארבעת שלבי היישום מתוך `approveHaluka.ts` (סימון `finished`+הצבעות → מכירות `splited` → חלוקות `ushar` → דלתות hervachti). `approveHaluka` קורא לו עכשיו במקום להחזיק עותק. זה כלל 7 ב-§6 בפועל: "כולם חתמו" ו"אף אחד לא התנגד בזמן" רצים על אותו קוד ולא יכולים להיפרד.
  - `src/routes/api/timegrama/tosplit.svelte` — המטפל. שער: `hasOk && !hasNo`, זהה ל-`decision.svelte`. `finished:true` ⇒ סגירה בלבד (D5, "אלא אם אורכב"). אין `hasOk` ⇒ אין מה להבשיל. יש `hasNo` ⇒ סגירה בלי יישום.
  - qid `173getTosplitForFinalize` + רישום ב-`qidsAccess.js` כ-`serviceAdmin` בלבד (קריאה של ה-cron).
  - `applyTosplitApproval.test.ts` — 11 טסטים.
  - **לא ממציאים הצבעות לשותקים.** נרשמות רק החתימות שניתנו בפועל; `finished` + השעון שפג הם מה שמעיד על השאר. עקבי עם `askm.svelte` ועם `offerToAssignee`.
  - **דלתות ה-hervachti מחושבות מחדש בשרת** מתוך `Tosplit.hervachti` (`hervachDeltasFrom`) ולא מתקבלות מהלקוח — לשעון אין לקוח, והשורות השמורות ממילא מקור אמין יותר.
- [ ] 4.3 `sheirutpend` → מיגרציה של `addSheirut.svelte` ל-Action (**B6**) + מטפל הבשלה.
- [ ] 4.4 `askwant` → מיגרציה של `sheirutShow.svelte` ל-Action (**B6**) + מטפל לפי **D2**: אישור של חבר ריקמה אחד לפחות + היעדר התנגדות ⇒ הבשלה; אף אישור ⇒ אין הבשלה. זה בדיוק `computeNegoGate`, לכן להשתמש בו ולא לכתוב שער חדש.
- [ ] 4.5 להסיר מהסכמה/מהשאילתה את 6 שדות היחס המתים, או לתעד למה הם נשארים.

### שלב 4.5 — מעבר ל-VPS (D3/D4) — ר׳ §4.6

- [ ] 4.5.1 להעביר את הרצת ה-cron ל-systemd timer / system cron על ה-VPS, כל 5-10 דקות.
- [ ] 4.5.2 לוודא שהוא פונה ל-`127.0.0.1` (Strapi מקומי) ולא ל-`tovmeod` דרך האינטרנט.
- [ ] 4.5.3 לסגור את `GET /api/timegrama` מבחוץ (loopback או header סוד) — **Q4/B10.2**.
- [ ] 4.5.4 להסיר את ה-`setTimeout` הארוך מ-`+server.js:107` — מיותר בתדירות הזו — **B7**.
- [ ] 4.5.5 לפרק את `/api/pingrama`; להעביר גם את `/api/monthi` לאותו timer.
- [ ] 4.5.6 לתעד ב-`docs/DEPLOY_API_DOCKER.md` כדי שה-timer ישרוד את הפריסה הבאה.

### שלב 5 — מחזור חיים ואמינות (סשן אחד + שינוי סכמה ב-Strapi)

- [ ] 5.1 הוספת `status`, `attempts`, `lastAttemptAt`, `lastError` ל-`Timegrama` ב-Strapi.
- [ ] 5.2 `npm run types:update`.
- [ ] 5.3 עטיפת כל מטפל ב-runner אחיד: `running` → מטפל → `done`/`failed` + `attempts++`.
- [ ] 5.4 backoff + תקרת ניסיונות + התראה ב-`failed` — **B9**.
- [ ] 5.5 נעילה רכה נגד ריצה כפולה — **B8**.

### שלב 6 — איחוד מול ה-Action System (2-3 סשנים, אחד לזן)

- [ ] 6.1 `PrincipalKind: 'timeout'` ב-`authz/types.ts` + טיפול ב-`applyAuthz` — **§4.1**.
- [ ] 6.2 `decision` → `voteOnDecision` (הכי גדול; להתחיל ממנו כי הוא הכי מסוכן).
- [ ] 6.3 `finiapruval` → `closeFiniapruval`.
- [ ] 6.4 `ask` → `finalizeAskAcceptance`.
- [ ] 6.5 `pendm` → `voteOnPendm`; `pmash` → `voteOnPmash`.
- [ ] 6.6 `stipend_payment` → `confirmStipendPayment`.
- [ ] 6.7 שינוי סיומות `.svelte` → `.ts` (הם `<script module>` בלבד, לא קומפוננטות).
- [ ] 6.8 מחיקת `SendToAdmin` הישיר מהמטפלים; מקור גישה אחד.

---

## 6. קונבנציה — איך מוסיפים זן timegrama חדש

> החלק הזה הוא **התוצר התיעודי** של המסמך. אחרי שלב 3 הוא הופך למחייב.

1. **קודם כול — האם יש Action?** אם ההבשלה עושה משהו שמשתמש יכול לעשות
   ידנית, ה-Action כבר קיימת. הבשלה = אותה Action עם principal `timeout`.
   אין Action ⇒ קודם כותבים אותה, לא כותבים לוגיקה בתוך `timegrama/`.
2. **`whatami` חייב להיות שם של יחס אמיתי** על `Timegrama` — הדיספצ'ר
   פותר אותו כמפתח (`element.attributes[whatami]`).
3. **לרשום ב-`kinds.ts`.** בלי זה הבדיקה תיפול. אין `if/else`.
4. **סוגרים תמיד את השעון.** כל נתיב יציאה — הצלחה, "כבר מטופל", "לא
   נמצא", "לא ראוי להבשלה" — מסתיים ב-`markDone`. היחיד שלא: שגיאה זמנית
   שראוי לנסות שוב עליה, וגם היא מגדילה `attempts`.
5. **בלי מחרוזות GraphQL מורכבות ידנית.** משתנים או `gqlStr()`. טקסט
   משתמש שנכנס למוטציה בלי escaping = תור תקוע לנצח.
6. **סבב נגדי מאפס את השעון** — סוגרים את הישן (`done:true`) ופותחים
   חדש. לא מעדכנים `date` על אותה רשומה, אחרת ריצה שכבר בדרך תבשיל את
   הגרסה הלא-נכונה.
7. **הבשלה חייבת להיות שקולה להסכמה מפורשת.** אותו קוד יישום לשני
   המסלולים (הדגם: `applyStandingVersion` ב-`archive/vote.ts`, שמשמש גם
   את ההצבעה וגם את `decision.svelte`). שני מסלולים = שתי אמיתות.
8. **חריגה מכלל 7 — כשהשתיקה אינה הסכמה.** יש מקרים שבהם צד אחד חייב
   לחתום מפורשות: המממן ב-stipend (`funderHasSigned`), והמיועד בהצעה
   מוקצית (`assignedOffer` ב-`ask.svelte:64`). לתעד את החריגה בקוד ובתוכנית
   הרלוונטית.

---

## 7. שאלות פתוחות להכרעה

**נענו (2026-08-25):**

| # | שאלה | ההכרעה |
|---|---|---|
| Q1 | האם `askwant` אמור להבשיל בשתיקה? | ✅ **D2** — כן, בתנאי שחבר ריקמה אחד לפחות אישר. |
| Q2 | `maap` — שתיקה מאשרת הוצאה כספית? | ✅ **D1** — רק אחרי חתימת האחראי; שתיקת השאר מבשילה. |
| Q3 | תדירות ה-cron | ✅ **D4** — 5-10 דקות, בשליטתנו על ה-VPS, בלי `setTimeout`. |
| Q4 | אימות ל-`GET /api/timegrama` | ✅ **D3** — כן; loopback או header סוד. שלב 4.5.3. |

**עדיין פתוחות:**

| # | שאלה | למה זה משנה |
|---|---|---|
| Q5 | האם להשאיר `.svelte` או לעבור ל-`.ts` (6.7)? | קוסמטי אבל מבלבל היום — הקבצים אינם קומפוננטות. |
| Q6 | `PrincipalKind: 'timeout'`, או `serviceAdmin` עם דגל בקונטקסט? | קובע את היקף 6.1. |
| **Q7** | **17 ה-`tosplit` התקועים מ-2025 — להבשיל למפרע, לסגור ולהודיע, או להשאיר?** | חלוקות רווח אמיתיות. החלטת מוצר. חוסם את 2.2. |

---

## 8. יומן ריצה

> לעדכן בכל סשן. מטרה: להמשיך מאיפה שעצרנו בלי לקרוא את כל המסמך מחדש.

| תאריך | שלב | מה נעשה | ממצאים |
|---|---|---|---|
| 2026-08-24 | — | מיפוי מלא של המערכת, כתיבת המסמך | 10 מטפלים, 4 זנים יתומים, 10 תקלות ממופות. B1 הוא החשוד המרכזי וטרם אומת. |
| 2026-08-25 | 0 ✅ | הרצת אבחון קריאה-בלבד מול הפרודקשן (`tg-diag.mjs`, `tg-diag2.mjs`) | ר׳ למטה. |
| 2026-08-25 | 1 ✅ | 10 תיקונים כירורגיים ב-4 קבצים: `+server.js`, `finiapp`, `pendM`, `ask` | B1/B3/B4/B5/B7/B10/B11 סגורות. 2 סטיות מכוונות מהתכנית (1.4, 1.8) מתועדות בשלב 1. `npm run check`: **0 שגיאות ו-0 אזהרות בקבצי timegrama**. `npm test`: 48 כשלים — **קיימים מראש**, אף קובץ טסט לא מייבא דבר מ-`routes/api/timegrama/` (אומת ב-grep). טרם נפרס, כלומר טרם רצה ריצה אמיתית. |
| 2026-08-25 | 4.1 ✅ | מטפל `maap` + חילוץ `settleCycleMaap`; הסרת ה-`autoApprove()` מ-`weget.svelte` | **בדרך התגלה B13 במלואו** (סריקת 44 הפעולות). המטפל הושאר מנותק עד שההרשאות הוענקו באותו יום; אומת ומחובר. 7 טסטים. dry-run: שתי שורות ה-`maap` ייסגרו **בלי שכסף יזוז** — האחראי (Asus) מעולם לא דיווח, וזה בדיוק D1. |
| 2026-08-25 | 4.2 ✅ | מטפל `tosplit` + חילוץ `applyTosplitApproval` המשותף | dry-run מול הפרודקשן חשף את **B12** (אין הרשאת `Haluka` לטוקן) והמטפל הושאר מנותק; ההרשאה הוענקה באותו יום, אומתה, והמטפל חובר. 15 טסטים עוברים, `npm run check` נקי בכל הקבצים שנגעתי בהם, `validate:qids` ללא שגיאות חדשות. **טרם נפרס.** ר׳ ה-dry-run למטה. |

### dry-run של מטפל ה-tosplit מול התור האמיתי (2026-08-25)

17 שעונים תקועים. **14 מהם מצביעים על חלוקה שכבר `finished`** — היא אושרה
מזמן דרך הקלף, ורק השעון לא נסגר. סגירה טכנית בלבד, ללא שום משמעות כספית.

**3 באמת יבשילו**, וזה כל מה שזז:

| tosplit | ריקמה | מי אישר | ההעברות שיהפכו לחייבות | מכירות |
|---|---|---|---|---|
| #10 | רביעי טאלי הודי | ברוך ✔ · david שתק 305 יום | david → ברוך **217.4** | — |
| #23 | Event by Click | רעות ✔ · ברוך ✔ · יניב שתק 68 יום | רעות → ברוך **357.85**, רעות → יניב **42.92** | 9 (סה"כ 2510.73) |
| #28 | טסט | ברוך ✔ · יענקלה ✔ · משתמש בדיקה שתק | ברוך → יענקלה **41** | 3 (סה"כ 41) |

בשלושתן `hervachti` ריק — שלב 4 של היישום הוא no-op, כך שהשינוי מצטמצם
לסגירת ה-Tosplit, סימון המכירות, וסימון ההעברות כמגיעות.

**נקודה שכדאי לומר במפורש:** ב-`whowhat.svelte` היוצר נרשם עם `what:true`
כבר ברגע היצירה, ולכן התנאי "לפחות אישור אחד" מתקיים תמיד ובפועל השער הוא
"אף אחד לא התנגד". זה בדיוק "שתיקה היא הסכמה" — אבל ב-#10 המשמעות היא
שריקמה של שניים שבה רק המציע חתם, והשני שתק 305 יום, מבשילה חוב של 217.4
על השותק. עקבי עם D2 ועם עקרונות-העל, ומצוין כאן כדי שזה ייעשה בעיניים
פקוחות.

### מה צפוי בריצה הראשונה אחרי הפריסה

29 שורות בתור. לפי מה שנבנה עד כה:

| תוצאה | # | מקור |
|---|---|---|
| ייסגרו (יעד מחוק) | 3 | B11 |
| ייסגרו (`archived` — כבר הוכרע) | 3 | B3 |
| ייסגרו (`tosplit` שכבר `finished`) | 14 | מטפל `tosplit` |
| **יבשילו — כסף זז** | **3** | מטפל `tosplit` (D5) |
| ייסגרו (`maap` — האחראי לא חתם, D1) | 2 | מטפל `maap` |
| יישארו פתוחות עד שלב 4.3/4.4 | 4 | `askwant` 3, `sheirutpend` 1 |

**25 מתוך 29 ייסגרו בריצה הראשונה, ורק 3 מהן מזיזות כסף** (שלוש חלוקות
הרווח, §8). 4 הנותרות — `askwant` ו-`sheirutpend` — ממתינות לשלבים 4.3/4.4,
שדורשים גם את מיגרציית ה-B6 בצד הלקוח.

**שתי שורות ה-`maap`:** שתיהן "עלות שרת" באותה ריקמה, 39 ו-20 יום באיחור,
והאחראי מעולם לא דיווח סכום. לפי D1 השעון נסגר והמחזור נשאר פתוח. שווה
לשים לב להשלכה: מרגע זה אין להם שעון, כלומר שום דבר לא יזכיר לאחראי. אם
רוצים תזכורת — זה מנגנון נפרד, לא הבשלה.

### מדידת שלב 0 — 2026-08-25

```
timegramas סה"כ ............................ 314
לא done .................................... 36
פג-תוקף + לא done (מה שה-cron רואה) ........ 29
done: null ................................. 0          ✅ B10 נקי
graphql.defaultLimit ....................... 250        ✅ B1 מופרך (29 < 250)
```

**פילוח סיבתי מלא של 29 השורות — אין "לא ידוע":**

| סיבה | # | זנים |
|---|---|---|
| **B2** — אין מטפל בכלל | **23** | `tosplit` 17, `askwant` 3, `maap` 2, `sheirutpend` 1 |
| **B11** — היעד נמחק, הדיספצ'ר מדלג בלי לסמן | **3** | `decision` #168 (381d), `ask` #192 (286d), `askm` #257 (83d) |
| **B3** — מטפל יצא ב-`archived` בלי `markDone` | **3** | `pmash` #17 (1040d), #18 (1033d), `finiapruval` #127 (522d) |

**מסקנות שמשנות את התכנית:**

1. **B1 יורד מ-🔴 ל-🟡.** ברירת המחדל היא 250 והתור הוא 29 — ה-cron רואה
   הכול. זו לא הסיבה. נשאר בתכנית כי התור גדל מונוטונית והתיקון הוא שורה.
2. **B2 הוא הגורם הדומיננטי — 79%.** מיקוד שלב 4 בהתאם.
3. **B11 היא תקלה חדשה שלא הייתה בטיוטה**, ואומתה 3 פעמים. תיקון של שורה.
4. **B5 ו-B10 אינם גורמים לאף שורה תקועה.** B5 רדום (ה-`why` היחיד שנבדק
   נקי), B10 נקי לגמרי.
5. **המערכת בבסיסה עובדת** — 278 מתוך 314 הבשילו כראוי. הבעיה היא זליגה
   שיטתית בשוליים, לא כשל כללי. זה מסביר למה זה לא נתפס עד היום.
6. **הכי מדאיג עסקית:** 17 הצעות חלוקת רווח (`tosplit`) שקיבלו שעון ומעולם
   לא הבשילו → **Q7**.

---

## 9. קבצים רלוונטיים

**ליבה**
- `src/routes/api/timegrama/+server.js` — הדיספצ'ר
- `src/routes/api/timegrama/*.svelte` — עשרת המטפלים
- `src/routes/api/pingrama/+server.js` — ה-proxy שה-cron החיצוני פוגע בו
- `src/routes/api/monthi/+server.js` — יוצר מחזורי `maap` + timegrama

**יוצרים (producers)** — ר׳ העמודה השנייה בטבלה §2.3

**לוגיקה משותפת שכבר עושה את זה נכון (דגמים לחיקוי)**
- `src/lib/server/archive/vote.ts` — `applyStandingVersion`
- `src/lib/server/stipend/apply.ts` — `applyStandingStipend`, `funderHasSigned`
- `src/lib/server/nego/negoGate.ts` — `computeNegoGate`
- `src/lib/server/actions/helpers/runResourceAskmAcceptance.ts`
- `src/lib/server/nego/timegrama.ts` — `ensureCandidacyTimegrama`

**תוכניות קשורות**
- `docs/PLAN_OBJECT_ARCHIVAL.md` · `docs/PLAN_STIPEND.md` ·
  `docs/PLAN_sale_holder_consent.md` · `docs/PLAN_NEGOTIATION_CANDIDATES.md` ·
  `docs/PLAN_API_PERMISSIONS.md` · `docs/PLAN_RECURRING_SALES.md`
