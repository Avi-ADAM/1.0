# דף הבית: סקשנים, הרחבה ודפי נושא — תוכנית ואודיט

> מסמך תכנון וביצוע. מאחד ארבעה אודיטים חיצוניים (ספטמבר 2026) עם בדיקה של כל טענה
> מול הקוד, וקובע מה נעשה, מה לא, ולמה.
>
> ענף: `claude/homepage-seo-sections-0wub3v`
> משלים את `PLAN_HOMEPAGE_MASTER.md` — שם הארכיטקטורה (גלילה ארוכה + 3D שמשתנה),
> כאן ה‑SEO והחלוקה לסקשנים/דפים.

## תוכן עניינים
1. [הבקשה והמסקנה](#1-הבקשה-והמסקנה)
2. [אימות האודיטים מול הקוד](#2-אימות-האודיטים-מול-הקוד)
3. [שלב א׳ — תיקוני SEO טכניים (בוצע)](#3-שלב-א--תיקוני-seo-טכניים-בוצע)
4. [שלב ב׳ — חלוקה לסקשנים ודפי מסלול](#4-שלב-ב--חלוקה-לסקשנים-ודפי-מסלול)
5. [שלב ג׳ — אשכולות SEO](#5-שלב-ג--אשכולות-seo)
6. [מה נשאר פתוח והחלטות שצריך לקבל](#6-מה-נשאר-פתוח-והחלטות-שצריך-לקבל)

---

## 1. הבקשה והמסקנה

**הבקשה:** לחלק את דף הבית לסקשנים ברורים, כל אחד בגודל מסך, עם אפשרות להרחבה בדף
עצמו ועוד יותר בדף נפרד לכל נושא.

**המסקנה: הכיוון נכון — עם שני תיקונים.**

ארבעת האודיטים הגיעו עצמאית לאותה אבחנה, וזו בדיוק התרופה שהבקשה מציעה:

| מקור | הניסוח |
|---|---|
| אודיט A | "דף ארוך מאוד… progressive disclosure, `learn more` לסקשנים ארוכים" |
| אודיט B | "ערבוב של שלושה מסרים שונים… כל אחד פונה לקהל אחר" |
| אודיט C | "מבחן 5 השניות" |
| אודיט D | "יותר מדי מוצרים באותו עמוד… כל בחירה תוביל לעמוד ממוקד משלה" |

וההערה בקוד עצמו, `fpage.svelte:317`, כבר אומרת את זה:
> *"The homepage was 23 sections that each opened cold… the thing both customers
> described as heavy and hard to understand."*

### תיקון 1 — לא לנעול על `100vh`

שלוש סיבות ספציפיות לקוד הזה:

- **חמש שפות** (he/en/ar/ru/es). אורך הטקסט שונה מהותית בין עברית לאנגלית; פריסה נעולה
  לגובה מסך תגלוש בשפה אחת ותישאר ריקה בשנייה.
- **`#text` הוא scroll container מקונן** (`overflow-y-auto`, `sm:w-1/2`) עם `#levi`
  קבוע (`fixed inset-0`) מאחור. `100vh` + `scroll-snap` בתוך קונטיינר מקונן נלחמים זה
  בזה, ובמובייל נוסף לזה ה‑browser chrome.
- **`prefers-reduced-motion`** כבר מכובד דרך `MotionControl` — snap אגרסיבי יסתור אותו.

**הכלל:** `min-height` בערך מסך, התוכן מחליט מהשם. רעיון אחד לסקשן, כותרת אחת, CTA אחד.
snap — רק בדסקטופ, ורק בכפוף ל‑`prefers-reduced-motion`.

### תיקון 2 — הבעיה היא הכמות, לא הגובה

היום 25 סקשנים ב‑2,123 שורות. 25 סקשנים בגודל מסך = 25 מסכי גלילה — החמרה, לא תיקון.
**היעד: 6–8 שערים בדף הבית**, כל אחד שער לנושא.

### התיקון החשוב מכולם — הציר הוא **קהל**, לא נושא

זו התובנה החדה ביותר מהאודיטים (D):

> יש כבר פילוח נכון בדף — *"יש לי שותפות / מחפש מיזם / רק רוצה לראות / דמו"* —
> **אבל כל הקהלים ממשיכים אחר כך באותו עמוד ארוך.**

הפילוח קיים ואז נזרק. הפיצול חייב להיות לפי קהל.

---

## 2. אימות האודיטים מול הקוד

### 2.1 טענות שכבר לא נכונות — אל תשקיעו בהן

| הטענה | המצב בפועל |
|---|---|
| שגיאות כתיב `colaboration / manegment / sistem` במטא | תוקן. `hooks.server.js:171-177` נקי בכל 5 השפות. |
| Title = "1💗1 הסכמה עולמית על חי \| ליצור יחד בהסכמה" | תוקן. היום `1💗1 · שותפויות הוגנות: משימות, שעות וחלוקת רווחים`. |
| "אין JSON‑LD" | היה `Organization` ב‑`app.html`. |
| "אין hreflang" | היה ב‑`app.html` — אבל שבור (§2.2). |
| "Sitemap מכיל 404ים" | נבדקו כל 20 הנתיבים — כולם מחזירים 200. |
| **"אין canonical → כפילות מול `1lev1.vercel.app`"** | **`svead`'s `Head` פולט `<link rel="canonical">` מוחלט.** דף הבית וכל דפי הגילוי (`/project`, `/gift`, `/availableMission`, `/demand`, `/availiableResorce`, `/maagad`) כבר היו מכוסים, ולכן גם עותק ה‑vercel הצביע חזרה על `1lev1.com`. |

> אודיטים B ו‑C לא גלשו לאתר אלא קראו מטמון גוגל. הם ניתוח **מסר** טוב, לא אודיט טכני.

### 2.2 טענות שאומתו בקוד

| # | הממצא | מיקום |
|---|---|---|
| 1 | **ה‑H1 היה הלוגו** — שלושה `<h1>` נפרדים: `1`, `💗`, `1`. הכותרת העסקית הייתה `<div>` מתחלף, כלומר לא כותרת כלל. | `fpage.svelte:615,623,631` |
| 2 | **hreflang קבוע שהצביע על דף הבית מכל דף באתר** — שישה תגים עם `href="https://1lev1.com/"` קשיח. כלומר `/why` הצהיר שהתרגום שלו הוא דף הבית. ה‑sitemap עשה זאת נכון per‑path, וה‑HTML סתר אותו. | `app.html` |
| 3 | **13 דפים ציבוריים בלי canonical.** רק `/why` הצהיר. השאר לא — כולל 5 שגם **לא הצהירו `<title>`** ולכן ירשו את הטייטל והתיאור של דף הבית: `/solution`, `/love`, `/kir`, `/give`, `/jenia`. | — |
| 4 | **המונים client‑only.** `onMount → /api/stat`, בלי `+page.server.js`. הזחלן קיבל `home.stats.loading` וארבעה אפסים — בדיוק מה שאודיט D תיאר. | `fpage.svelte:297` |
| 5 | **`robots.txt` בלי שורת `Sitemap:`** | `static/robots.txt` |
| 6 | **דפי נושא חסרים מה‑sitemap:** `/why`, `/uses` (וגם `/solution`, `/deals` — ר' §6). | `sitemap.xml/+server.js` |
| 7 | **`/he`, `/en`, `/ar` רינדרו עותק זהה לבייט של דף הבית**; `/ru`, `/es` החזירו 404. הם לא חלק מסכמת ה‑`?lang=` שכל שאר האתר מסכים עליה. | `routes/{he,en,ar}` |
| 8 | **`meta robots` קשיח `index, follow`** מעל `%sveltekit.head%` — כלומר דף שצריך `noindex` לא יכול להחליף אותו, ונוצרות שתי הנחיות סותרות. | `app.html` |
| 9 | **`/solution` ו‑`/kir` הם טיוטות placeholder** — `"עוד ועוד"`, `"נקודת ההתחלה"`, שורות `"a"` — ובלי `<title>` הן הופיעו בחיפוש תחת הטייטל של דף הבית. | — |

---

## 3. שלב א׳ — תיקוני SEO טכניים (בוצע)

| # | מה | קבצים |
|---|---|---|
| 1 | H1 יחיד ואמיתי. הלוגו הפך ל‑`<span>` תחת `aria-hidden`; נוסף `<h1>` קבוע עם `home.hero.h1` ב‑5 שפות. הרוטציה נשארה כתת‑כותרת — **רוטציה לא יכולה להיות H1**: מה שמורכב ברגע הסריקה הוא מה שנאנדקס, כלומר הגרלה בין חמש הבטחות. | `fpage.svelte`, `translations/*/home.json` |
| 2 | hreflang נבנה per‑path ב‑`hooks.server.js` דרך `%hreflang%`. אותה סכמה כמו ה‑sitemap: עברית = הכתובת החשופה + `x-default`, השאר `?lang=`. | `app.html`, `hooks.server.js` |
| 3 | `<link rel="canonical" href="%canonical%">` בבלוק ה‑fallback (אחרי `%sveltekit.head%`, כמו ה‑title) — דף שמצהיר משלו מנצח, וכל השאר מקבל self‑canonical. מכסה את כל 13 הדפים במכה אחת. | `app.html` |
| 4 | `+page.server.js` שמביא את המונים ב‑SSR דרך `/api/stat`. כשל Strapi מחזיר `stats: null` והרכיב נופל חזרה ל‑fetch של הלקוח — **הכשל לעולם לא מפיל את דף הבית**. | `+page.server.js`, `+page.svelte`, `fpage.svelte` |
| 5 | `Sitemap:` ב‑`robots.txt`. | `static/robots.txt` |
| 6 | `/why` ו‑`/uses` נוספו ל‑sitemap. | `sitemap.xml/+server.js` |
| 7 | `/he /en /ar /ru /es` → **301** ל‑`/?lang=xx`. שלושת קבצי המסלול נמחקו. 301 ולא 303 כדי שהקישורים יתאחדו על הכתובת הקנונית. | `hooks.server.js`, מחיקת `routes/{he,en,ar}` |
| 8 | הוסר ה‑`meta robots` הקשיח מ‑`app.html` — `index, follow` הוא ברירת המחדל ממילא, והתג רק סתר דפים שרוצים `noindex`. | `app.html` |
| 9 | `noindex, follow` ל‑`/solution` ו‑`/kir` עד שיהיה בהם תוכן אמיתי. | שני הדפים |
| 10 | `SoftwareApplication` JSON‑LD לצד ה‑`Organization` הקיים — זו הישות שחיפוש "מערכת לניהול שותפות" מחפש. | `app.html` |

### אימות שבוצע

- `npm run check` — **0 שגיאות חדשות** מול baseline (הושווה עם `git stash`; אותן 6 שגיאות
  קיימות ב‑`fpage`/`hooks`, ואותן 5 ב‑`/solution`, רק בשורות מוסטות).
- `npm test` — `12 failed | 148 passed` לפני **ואחרי**, זהה. הכשלים הקיימים ב‑
  `TaskApprovalButton` ו‑`server/actions/*` ואינם נוגעים לשינוי.
- `npm run check:i18n` ✓ · `check:script` ✓ · `check:proxy` ✓
- **אימות HTML חי** מול `vite dev`:
  - `/` — `<h1>` יחיד; hreflang מצביע על `/` ולא על הבית בלבד.
  - `/he→/`, `/en→/?lang=en`, `/ar`, `/ru`, `/es` — כולם **301**.
  - `/about`, `/uses`, `/guid`, `/faq`, `/love`, `/kir`, `/solution` — כולם קיבלו canonical.
  - `/solution`, `/kir` — `noindex` יחיד, בלי סתירה.

> `npm run build` נכשל בסביבה הזו על `$env/static/private` חסר (אין `.env`) — מגבלת
> סביבה שקדמה לשינוי, לא רגרסיה.

---

## 4. שלב ב׳ — חלוקה לסקשנים ודפי מסלול

### שלושת הרבדים

| רובד | מה יש בו | הכלל הקריטי |
|---|---|---|
| **L1** — סקשן בדף הבית | משפט אחד + ויזואל אחד + CTA אחד | ~גובה מסך, לא נעול |
| **L2** — הרחבה במקום | 2–3 פסקאות, `<details>`/אקורדיון | **חייב להיות ב‑DOM ב‑render** (מוסתר ב‑CSS), לא fetch בלחיצה — אחרת גוגל לא רואה אותו |
| **L3** — דף נפרד | הסיפור המלא, screenshots, FAQ ייעודי | **לא העתק של L1/L2** — אחרת duplicate content שמתחרה בדף הבית |

חלוקת התפקידים: **דף הבית = ההבטחה. דף הנושא = ההוכחה והעומק.**

### דף הבית — 7 שערים (מ‑25 סקשנים)

| # | שער | תוכן קיים למחזור | יעד L3 | מצב 3D (§5.3 ב‑MASTER) |
|---|---|---|---|---|
| 0 | הדר sticky + scroll‑spy | `scrollToId` כבר קיים | — | — |
| 1 | Hero — H1 + CTA + מונים חיים | בוצע ב‑§3 | — | `1lev1.svelte` |
| 2 | **מפצל הקהלים** — וכל בחירה **עוזבת את הדף** | `#who` קיים | 3 דפי מסלול | תנועה קלה |
| 3 | הנוסחה + המחשבון | `SplitCalculator`, `#split` | `/split` | חבילות→מטבעות |
| 4 | הצצה למוצר | `ProductPeek`, `#features` | `/uses` | — |
| 5 | קונסיירז' | `#concierge` | `/concierge` | `Scene.svelte` |
| 6 | אמון: מונים + הסכמה + FAQ | `#faq`, `/love` | `/faq`, `/hascama` | גלובוס |
| 7 | CTA סופי + Footer מלא | — | — | התכנסות |

### שלושת דפי המסלול (L3 ראשי)

| מסלול | כתובת | הקהל | ה‑CTA |
|---|---|---|---|
| יש לי שותפות | `/partnership` | שותפים שרבים על 50/50 | "נסו את החלוקה שלכם" |
| מחפש מיזם | `/join` | פרילנסרים, מחפשי צד | "מצאו מיזם שמתאים לכישורים" |
| במקום בוס | `/no-boss` | שכירים מתוסכלים | "ספרו במה אתם טובים" |

**`/uses` כבר עושה חלק מהעבודה** (הערת הפתיחה שלו: *"Six doors… he could not tell
whether it applied to his business"*). לבנות עליו, לא לצדו.

### `/no-boss` — שתי אזהרות מחייבות

שני אודיטים עצמאיים הגיעו לאותה אזהרה, והקוד מאשר אותה:

1. **אל תוביל לרשימה ריקה.** אודיט B: *"אם אחרי ההרשמה המשתמש נוחת בפלטפורמה ריקה —
   תצא בהפסד כפול"*; אודיט D: *"אל תוביל לרשימה ריקה"*. ה‑CTA חייב להיות
   *"ספרו במה אתם טובים ונחבר אתכם ידנית"*, **לא** `/availableMission`.
   תנאי סף לקמפיין ממומן: 5–10 ריקמות פעילות אמיתיות.
2. **אל תבנה על שנאה.** דף שכל מסרו "הבוס מנצל אותך" מושך מתלוננים, ושותף גרוע
   בריקמה פוגע במוצר. הנוסחה: כאב בכותרת, פתרון חיובי בכל השאר.

### כללי מימוש מחייבים

- **i18n:** כל טקסט חדש דרך `$t('ns.key')` ב‑5 שפות. `{ he: …, en: … }` ברכיב הוא רגרסיה
  (ר' `CLAUDE.md`) — וזו בדיוק התקלה ב‑`/solution` היום.
- namespace חדש → רישום ב‑`ROUTED` ב‑`routes.js`, ואז **`npm run check:i18n`**. בלעדיו
  הטקסט פשוט לא מוצג, בלי שגיאה.
- כל דף חדש: `<title>` + `description` משלו, H1 יחיד, כניסה ל‑sitemap. ה‑canonical
  וה‑hreflang כבר מטופלים גלובלית אחרי §3.
- **`npm run check:script`** אחרי כל עריכה בעברית/ערבית/קירילית.

### ה‑3D

חלוקה לסקשנים ברורים היא בדיוק מה שמאפשר את §5.3 ב‑`PLAN_HOMEPAGE_MASTER.md`:
מעבר מ‑`scrolli` (boolean, 150ms) ל‑`scrollProgress` + `activeSection` עם
IntersectionObserver. הרווח כפול — לכן כדאי לעשות את שניהם באותו מהלך.

---

## 5. שלב ג׳ — אשכולות SEO

דף אחד לכל **כוונת חיפוש**, לא דף אחד שמנסה הכל:

| כוונת חיפוש | דף |
|---|---|
| ניהול שותפות עסקית | `/partnership` |
| חלוקת רווחים בין שותפים | `/split` (+ המחשבון) |
| חלוקה דינמית במקום 50/50 | מאמר + מקרה בוחן |
| מעקב שעות בין שותפים | עמוד יכולת |
| איך למצוא שותף עסקי | `/join` |
| עבודה בלי בוס | `/no-boss` |
| ניהול שותפות באקסל | עמוד השוואה |

בנוסף: `FAQPage` JSON‑LD ב‑`/faq`, `BreadcrumbList` בדפים פנימיים.

---

## 6. מה נשאר פתוח והחלטות שצריך לקבל

1. **`/` מקבל canonical של `?lang=en` כשאין `Accept-Language`.** `svead` לוקח את
   `home.pageUrl`, שנגזר מהשפה שנפתרה. זו התנהגות שקדמה לשינוי והיא עקבית עם טבלת
   ה‑hreflang, אבל שווה החלטה: האם הכתובת החשופה `/` צריכה תמיד להיות canonical של
   עצמה. **לא נגעתי בזה** — זה משנה איזו גרסה נאנדקסת.
2. **`/deals` הוצא מה‑sitemap** — הוא מחזיר 302 לאורח (מאחורי התחברות), ודף כזה
   ב‑sitemap הוא soft‑404 בעיני זחלן.
3. **`/solution` ו‑`/kir` קיבלו `noindex`** כי הם placeholder. אם מתכננים לסיים אותם
   בקרוב — להסיר את התג ולהוסיף `<title>` וכניסה ל‑sitemap.
4. **`/give` ו‑`/jenia`** — טפסים/כלים, לא דפי שיווק. אין להם `<title>`; היום הם
   מקבלים self‑canonical אבל עדיין יורשים את הטייטל של דף הבית. להחליט: טייטל משלהם
   או `noindex`.
5. **`/guid` ו‑`/availiableResorce`** — שגיאות כתיב בכתובות. תיקון דורש 301 + עדכון כל
   הקישורים הפנימיים וה‑sitemap. **לא נגעתי** — זה שינוי רחב שראוי לענף משלו.
6. **קונסיירז' — דיוק בקופי.** לפי `PLAN_HOMEPAGE_MASTER.md §7.1` ליבת המנוע עדיין
   ברובה mock. הקופי חייב להבטיח מה שרץ היום.
7. **הבטחות חזקות מדי** ("ביטחון כלכלי", "97% הכנסה גם אחרי הפסקת עבודה") — אודיט D
   ממליץ שעו"ד יעבור עליהן לפני קמפיין ממומן. מחוץ לתחום ההנדסי, בתוך תחום הסיכון.
8. **הוכחה חברתית** — כל ארבעת האודיטים ציינו את היעדרה. זה תוכן, לא קוד: 3–5 עדויות
   עם שם ותפקיד, וסיפור מקרה אחד עם מספרים.
