# Generated by Cargo
/target/

# Generated by tauri-build at compile time. `gen/android` is deliberately NOT
# ignored — we hand-edit AndroidManifest.xml, gradle and res/ there (splash,
# icons, Hebrew app label), so it is project source, not build output.
/gen/schemas/

# Android build output inside the committed gen/android project
/gen/android/.gradle/
/gen/android/build/
/gen/android/local.properties
/gen/android/app/build/
/gen/android/app/.cxx/
/gen/android/app/src/main/jniLibs/

# Local LAN dev override (copy of tauri.dev-local.json.example)
/tauri.dev-local.json

# ── Secrets — never commit ───────────────────────────────────────────────────
*.jks
*.keystore
keystore.properties
google-services.json
*.p8
