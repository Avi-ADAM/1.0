import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'world.1lev1.app',
  appName: '1lev1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
